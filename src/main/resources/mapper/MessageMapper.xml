<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.onik.flowticket.mapper.MessageMapper">

    <!-- 插入消息通知。 -->
    <insert id="insert" parameterType="com.onik.flowticket.entity.Message" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO message (receiver_id, sender_id, ticket_id, message_type, title, content, is_read, create_time, deleted)
        VALUES (#{receiverId}, #{senderId}, #{ticketId}, #{messageType}, #{title}, #{content}, 0, NOW(), 0)
    </insert>
</mapper>
